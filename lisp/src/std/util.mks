const unreachable = [T] () -> T => (
    panic("unreachable was reached");
    uninitialized
);

const Int_to_Float64 = @call "C" (x :: Int) -> Float64 => (
    @native "(double)\(x)"
);

const div = [T] @call "C" (a :: T, b :: T) -> T => (
    @native "\(a) / \(b)"
);
